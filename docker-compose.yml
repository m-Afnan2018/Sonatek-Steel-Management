services:
  mongo:
    image: mongo:7
    container_name: sonatek_mongo
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db
    networks:
      - sonatek_net

  backend:
    build: ./backend
    container_name: sonatek_backend
    restart: unless-stopped
    env_file: .env
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGO_URI=mongodb://mongo:27017/sonatek_pm
    depends_on:
      - mongo
    ports:
      - "5000:5000"
    volumes:
      - uploads_data:/app/uploads
    networks:
      - sonatek_net

  frontend:
    build:
      context: ./frontend
      args:
        # NEXT_PUBLIC_* vars must be set at build time — they are baked into the JS bundle
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:5000/api}
    container_name: sonatek_frontend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    depends_on:
      - backend
    ports:
      - "3000:3000"
    networks:
      - sonatek_net

volumes:
  mongo_data:
  uploads_data:

networks:
  sonatek_net:
